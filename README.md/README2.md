# 🛠️ สรุปคำสั่ง Git (ภาษาไทย)

คู่มือคำสั่ง Git ที่ใช้บ่อย พร้อมคำอธิบายและสีสันอ่านง่าย

---

## 📝 ตรวจสอบเวอร์ชัน Git

```bash
git -v
```
| **ตรวจสอบเวอร์ชันของ Git ที่ติดตั้งในเครื่อง**

---

## 🧑‍💻 ตั้งค่าผู้ใช้ Git

```bash
git config --global user.name "[ชื่อผู้ใช้ GitHub ของคุณ]"
```
| **ตั้งค่าชื่อผู้ใช้ GitHub ในคอมพิวเตอร์ของคุณ**

```bash
git config --global user.email "[อีเมล GitHub ของคุณ]"
```
| **ตั้งค่าอีเมล GitHub ในคอมพิวเตอร์ของคุณ**

```bash
git config --global credential.helper
```
| **ตั้งค่าให้ Git จำชื่อผู้ใช้และอีเมลในเครื่อง**

```bash
git config --list
```
| **ตรวจสอบการตั้งค่าปัจจุบัน (ชื่อผู้ใช้ & อีเมล)**

---

## 📂 เริ่มต้นใช้งาน & เพิ่มโค้ดลง GitHub

```bash
git init
```
| **สร้าง Git repository ใหม่ในโปรเจ็กต์**

```bash
git add .
```
| **เพิ่มไฟล์ทั้งหมดในโปรเจ็กต์เข้าสู่ github**

```bash
git commit -m "[ข้อความเกี่ยวกับไฟล์ที่เพิ่ม]"
```
| **บันทึกการเปลี่ยนแปลงพร้อมข้อความอธิบาย**

```bash
git branch -M main
```
| **เปลี่ยนชื่อ branch เป็น `main`**

```bash
git remote add origin [URL ของ GitHub repo]
```
| **เชื่อมต่อ repository ในเครื่องกับ GitHub**

```bash
git remote -v
```
| **ตรวจสอบ remote repositories ที่เชื่อมต่ออยู่**

```bash
git push -u origin main
```
| **อัปโหลดโค้ดขึ้น GitHub และตั้งค่า branch เริ่มต้น**

---

<details>
  <summary>🎨 <b>Tips สำหรับ Markdown สีสัน</b></summary>
  
  - ใช้ **ตัวหนา** เพื่อเน้นคำสั่งสำคัญ
  - เพิ่มอีโมจิสำหรับการนำสายตา
  - ใช้ code block เพื่อไฮไลท์ภาษา
  - จัดระเบียบด้วยหัวข้อและเส้นแบ่ง
</details>